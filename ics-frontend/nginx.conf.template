server {
  listen ${PORT};
  server_name _;

  location = /config.js {
    default_type application/javascript;
    return 200 'window.__ENV__ = { API_BASE_URL: "${API_BASE_URL}", API_KEY: "${API_KEY}" };';
  }

  root /usr/share/nginx/html;

  location / {
    try_files $uri /index.html;
  }
}
